// Where the API lives. The default matches the `http` launch profile of
// notes-api run locally; point it at your hosted instance for a release
// build (Android needs the machine's LAN address, not localhost).
export const API_URL = 'http://localhost:5011/api/notes';
